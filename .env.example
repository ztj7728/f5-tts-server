# --- Server Configuration ---
# The host the server will listen on. Use 0.0.0.0 to be accessible from the network.
SERVER_HOST="0.0.0.0"
# The port the server will listen on.
SERVER_PORT=8000

# --- URL Configuration ---
# The public-facing hostname (including http/https and port) to use when generating audio URLs.
# This is what users will see in the API response.
# Example: http://your-domain.com or http://123.45.67.89:8000
PUBLIC_HOSTNAME="http://127.0.0.1:8000"
